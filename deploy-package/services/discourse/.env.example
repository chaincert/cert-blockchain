# CERT Discourse Environment Configuration
# Copy to .env and fill in your values

# SMTP Configuration (required for Discourse)
SMTP_ADDRESS=smtp.mailgun.org
SMTP_PORT=587
SMTP_USER=postmaster@mail.c3rt.org
SMTP_PASSWORD=your_smtp_password_here

# Database
DB_PASSWORD=generate_secure_password_here

# DiscourseConnect SSO Secret
# Generate with: openssl rand -hex 32
SSO_SECRET=generate_sso_secret_here

# Domain (for nginx config)
DISCOURSE_DOMAIN=forum.c3rt.org

