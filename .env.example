# CERT Blockchain Environment Configuration
# Copy this file to .env and configure for your environment

# Database Configuration
POSTGRES_PASSWORD=your-secure-password-here

# API Configuration
JWT_SECRET=

# Testnet UX configuration
# Fixed APY shown on the web dashboards (percent)
TESTNET_STAKING_APY_PERCENT=10


# IPFS Configuration (use local or external gateway)
IPFS_GATEWAY=https://ipfs.c3rt.org

# Chain Configuration
CERT_CHAIN_ID=cert-testnet-1
CERT_MONIKER=cert-node-1

# certd tx signing/broadcast (used by the main Go API for POST create endpoints)
# Defaults used by docker-compose (can be overridden for other networks)
CERT_TX_CHAIN_ID=951753
CERT_TX_FROM=validator
CERT_TX_KEYRING_BACKEND=test
CERT_TX_HOME=/root/.certd
CERT_TX_NODE=tcp://localhost:26657
CERT_TX_GAS=200000
CERT_TX_FEES=10000ucert
# Alternative to CERT_TX_FEES:
# CERT_TX_GAS_PRICES=0.025ucert
CERT_TX_BROADCAST_MODE=block

# Network Configuration
# Set these for production deployment
# CORS_ORIGINS=https://app.c3rt.org,https://c3rt.org

# Optional: External RPC endpoints (for bridge validator)
# ETHEREUM_RPC_URL=https://mainnet.infura.io/v3/YOUR_API_KEY
# BSC_RPC_URL=https://bsc-dataseed.binance.org

# Optional: Logging Configuration
# LOG_LEVEL=info
# LOG_FORMAT=json
